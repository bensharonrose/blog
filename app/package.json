{
  "name": "blog",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "^5.0.2",
    "http-proxy-middleware": "^1.0.4",
    "react": "^16.13.1",
    "react-bootstrap": "^2.0.0-beta.4",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.1",
    "styled-components": "^5.1.1"
  },
  "scripts": {
    "startLinux": "PORT=8000 react-scripts start --disable-host-check",
    "start": "set PORT=3000 && react-scripts start",
    "build": "react-scripts build",
    "testLinux": "rm -rf ./test-report.xml && CI=true ./node_modules/.bin/react-scripts test --verbose --env=jsdom --testResultsProcessor ./node_modules/jest-junit-reporter",
    "test": "./node_modules/.bin/react-scripts test --verbose --testResultsProcessor ./node_modules/jest-junit-reporter",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "enzyme": "^3.10.0",
    "enzyme-adapter-react-16": "^1.15.1",
    "jest": "^24.9.0",
    "jest-junit-reporter": "1.1.0",
    "jest-localstorage-mock": "^2.4.2",
    "junit-merge": "^2.0.0",
    "web-vitals": "^1.1.0"
  }
}
